#ifndef DATE
#define DATE

// This struct is just three numbers put together, given a meaning. It does not have any methods, initializer or destructor.
typedef struct {
	int year;
	int month;
	int day;
} Date;

#endif
